# Andrew Ikovic Blog

This site is only HTML and CSS.

## Add a post

1. Copy `posts/first-post.html`.
2. Rename the copy, for example `posts/my-new-post.html`.
3. Edit the title, date, and writing inside the file.
4. Add a new post preview link in `index.html`.

There is no build step.
